## Linux 的 wc 命令 
### 简介

打印每个 FILE 的`换行符`、`字数`和`字节数`，如果指定了多个 FILE，则打印总行数。没有 FILE，或者当 FILE 为`-`时，读取`标准输入`（stdin）。

### 语法

``` bash
wc [OPTION]... [FILE]...
wc [OPTION]... --files0-from=F
```

参考文献：

\> [https://linux.die.net/man/1/wc](https://linux.die.net/man/1/wc)
